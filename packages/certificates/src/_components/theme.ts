/** Hex tokens aligned with `@sycom/emails` theme (see `_components/theme.tsx` there). */
export const certificateColors = {
  background: "#ffffff",
  foreground: "#1c1c1e",
  muted: "#838386",
  border: "#e8e8e6",
  primary: "#1a1a6e",
} as const;

/** Facet palette for the corner artwork, derived from the navy primary. */
export const certificateArtworkColors = {
  navy: "#1a1a6e",
  indigo: "#3d3da8",
  violet: "#6f5bd6",
  teal: "#2a8f9e",
} as const;
